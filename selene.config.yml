---
base: lua51
globals:
  # PLUGIN and RUNTIME are injected by mise when the plugin is activated.
  PLUGIN:
    any: true
  RUNTIME:
    any: true
  # Selene's Lua 5.1 standard does not model the standard error file handle.
  io.stderr:
    any: true
